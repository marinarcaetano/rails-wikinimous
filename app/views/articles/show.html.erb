<h1><%= @article.title %></h1>
<p><%= @article.content %> </p>

<%= link_to "Delete #{@article.title}",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<%= link_to "Edit", edit_article_path(@article)%>
